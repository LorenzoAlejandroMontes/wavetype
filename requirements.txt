# Core
faster-whisper
sounddevice
numpy
httpx
Pillow
keyboard
pywin32
comtypes
pycaw

# Optional: FLAC encoding before upload (smaller, faster uploads).
# Without it, audio is sent as WAV.
av

# Live words next to the caret (Nemotron streaming on the CPU). The model itself (~475 MB) is
# downloaded by the app on first run into models/ (model_fetch.py).
sherpa-onnx>=1.13.8

# Optional: old Italian-only live-preview engine.
# vosk
